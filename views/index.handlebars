<div class="jumbotron">
  <h1 class="display-4">Bem-vindo ao BlogApp!</h1>
  <p class="lead">Este é um blog simples, desenvolvido com Node.js + Express + MongoDB</p>
  <hr class="my-4">
  <p>Aqui você vai ler sobre diversos tipos de assuntos</p>
  <a class="btn btn-primary btn-lg mt-2" href="/usuarios/registro" role="button">Crie uma conta</a>
</div>

<hr>

<h2>Postagens recentes: </h2>

{{#each postagens}}
    <div class="card mt-4">
        <div class="card-body">
            <h3>{{titulo}}</h3>
            <p>{{descricao}}</p>
            <a href="/postagem/{{slug}}"><button class="btn btn-success">Leia mais</button></a>
            <hr>
            <small>Categoria: {{categoria.nome}}</small><br>
            <small>Data de publicação: {{date}}</small>
        </div>
    </div>
{{else}}
    <h5>Nenhuma Postagem</h5>
{{/each}}